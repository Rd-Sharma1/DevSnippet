import React from "react";
import { StyleSheet, Text, View } from "react-native";

const createSnippets = () => {
  return (
    <View>
      <Text>createSnippets</Text>
    </View>
  );
};

export default createSnippets;

const styles = StyleSheet.create({});
